<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>View Profile</title>
</head>
<body>
    <h1>View Profile</h1>
    <div>
        <h2>Profile Information</h2>
        <p><strong>Username:</strong> <%= user.username %></p>
        <p><strong>Email:</strong> <%= user.email %></p>
        <p><strong>Phone:</strong> <%= user.phone %></p>
        <p><strong>Address:</strong> <%= user.address %></p>
        <% if (user.picture) { %>
            <img src="/uploads/profile_pictures/<%= user.picture.split('/').pop() %>" alt="Profile Picture">
        <% } %>
        <a href="/<%= user.role %>/profile/edit">Edit Profile</a>
    </div>
</body>
</html>
